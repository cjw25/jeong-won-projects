# T10 실험 설계 변경 기록

## 2026-09-22 실행 환경 수정

- 변경 전:
  run_all.ps1 실행 시 P01_web의 CycloneDX SBOM 생성까지는 성공했으나
  raw/sbom 출력 디렉터리를 찾지 못해 Copy-Item 단계에서 중단되었다.

- 변경 후:
  run_all.ps1이 raw, raw/sbom, raw/osv, analysis 디렉터리를
  실행 전에 명시적으로 생성하도록 수정하였다.

- 변경 이유:
  생성된 SBOM을 원자료 폴더에 저장하기 위한 실행 경로 오류를 수정하기 위해서다.

- 실험 설계 영향:
  없음.

- 유지된 조건:
  10개 프로젝트, 라이브러리 및 버전, CycloneDX 방식,
  OSV 조회 방식, 독립변수, 종속변수 및 표본 수는 변경하지 않았다.

- 데이터 처리:
  경로 오류가 해결된 후 10개 프로젝트 전체 실험을 처음부터 동일 조건으로 실행하였다.